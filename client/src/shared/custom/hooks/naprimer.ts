// import { setRangeFilter } from "@/entities/projects/model/projectSlice";
// import { useAppDispatch, useAppSelector } from "@/shared/library/hooks";

// export const naprimerHook = () => {
//   const loading = useAppSelector((s) => s.project.loading);
//   useAppDispatch()(setRangeFilter(loading ? 0 : 100));
// };
